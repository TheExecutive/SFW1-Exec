// js goes her/

var exec = function() {
	return new exec.prototype.init();
};

exec.prototype = {
	init: function() {
		//init the library	
	}
};

exec.prototype.init.prototype = exec.prototype;
//this is what allows the "factory" technique to work
